import HomeHeader from '@/components/islets/home-page/home-header';
import HomePageWrapper from '@/components/layout/home-page/home-page-wrapper';

const HomePage = () => {
  return (
    <HomePageWrapper>
      <HomeHeader />
    </HomePageWrapper>
  );
};

export default HomePage;
