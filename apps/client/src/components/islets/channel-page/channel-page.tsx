import ChannelPageHeader from '@/components/islets/channel-page/channel-page-header';
import PageWrapper from '@/components/layout/channels-page/page-wrapper';

const ChannelPage = () => {
  return (
    <PageWrapper>
      <ChannelPageHeader />
    </PageWrapper>
  );
};

export default ChannelPage;
